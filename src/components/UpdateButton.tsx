import React from "react";
import { CiEdit } from "react-icons/ci";

const UpdateButton = () => {
  return (
    <button>
      <CiEdit size={25} />
    </button>
  );
};

export default UpdateButton;
